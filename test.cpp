#include "beidanci.h"

using namespace std;

int main()
{
	/* string meaning;
	cin >> meaning;
    Word * word = new Word("me", meaning, 1);
	word->addSentence("Please look at me.");
	word->addSentence("Do you want to talk with me?");
    cout << word->getName() << endl;
    cout << word->getExplain() << endl;
    cout << word->getLevel() << endl;
	for(int i = 0;i < word->getSentencesSize();i++){
		cout << word->getSentences(i) << endl;
	} */
	
	//Dict dictionary("data/_gre.txt");
	//Dict dictionary;
/* 	Counter cc("test.txt");
	cc.count(*Gre::getInstance()); */
/* 	string name = Cet4::getInstance()->randomWord(0);
	string name1;
	char c1[100], c2[100];
	cout << name << endl;
	cin.getline(c1, 100);
	cin >> name1; */
	//cin.getline(c2, 100);
	//string s1(c1);
	//string s2(c2);
/* 	Cet4::getInstance()->searchWordEx(name);
	Cet4::getInstance()->addWordSenten(name, s1);
	Cet4::getInstance()->searchWordSe(name); */
	//dictionary.addWordSenten(name, s2);
/* 	Cet4::getInstance()->searchWordSe(name1);
	Cet4::saveInstance(); */
	//cout << dictionary.searchWordLevel(name) << endl;
	Beidanci b;
/* 	while(login() ){
		b.run();
	} */
	b.run();
	return 0;
}